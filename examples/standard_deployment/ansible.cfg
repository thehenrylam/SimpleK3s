[defaults]

inventory = ./inventory.yml

roles_path = ./roles

forks = 10

# Render a failed task's stdout/stderr as real text instead of one JSON line with escaped newlines — a failed `tofu apply` is unreadable otherwise.
stdout_callback = ansible.posix.debug
